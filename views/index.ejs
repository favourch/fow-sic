<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Focus Areas</title>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background: #f0f2f5;
      color: #333;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      padding: 20px;
      box-sizing: border-box;
    }
    .main-title {
      text-align: center;
      margin-bottom: 20px;
    }
    .main-title h1 {
      font-size: 2.5em;
      color: #201f1f;
      margin: 5px 0;
    }
    .main-title h3 {
      font-size: 1.5em;
      color: #201f1f;
      margin: 5px 0;
    }
    .tile {
      display: inline-block;
      width: 100%;
      max-width: 300px;
      height: 100px;
      margin: 10px; /* Adjust margin for better spacing */
      border-radius: 5px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      background: linear-gradient(to right, #201f1f, #242020);
      color: white;
      font-size: 1.2em;
      font-weight: bold;
      text-align: center;
      line-height: 100px; /* Center text vertically */
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .tile:hover {
      transform: scale(1.1);
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
    }
    @media (max-width: 576px) {
      
      .tile {
      display: inline-block;
      width: 100%;
      max-width: 300px;
      height: 100px;
      margin: 10px; /* Adjust margin for better spacing */
      padding: 10px; /* Adjust margin for better spacing */
      border-radius: 5px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      background: linear-gradient(to right, #201f1f, #242020);
      color: white;
      font-size: 15px;
      font-weight: bold;
      text-align: center;
      line-height: 100px; /* Center text vertically */
      transition: transform 0.3s, box-shadow 0.3s;
      }
    }
  </style>
</head>
<body>
  <div class="container text-center">
    <div class="main-title">
      <h1>Future Of Work Sustainability Innovation Camp</h1>
      <h3>Focus Areas</h3>
    </div>
    <div class="tile-container row justify-content-center">
      <% focusAreas.forEach(area => { %>
        <div class="col-6 col-sm-6 d-flex justify-content-center mb-2">
          <div class="tile"><%= area.name %></div>
        </div>
      <% }); %>
    </div>
    <form action="/create-groups" method="POST" class="mt-4">
      <div class="form-group">
        <label for="groupCount">Number of Groups (max 10):</label>
        <input type="number" id="groupCount" name="groupCount" class="form-control" min="1" max="10" required>
      </div>
      <button type="submit" class="btn btn-dark">Create Groups</button>
    </form>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
