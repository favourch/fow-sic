<!DOCTYPE html>
<html>
<head>
  <title>Groups</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background: #f0f2f5;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      text-align: center;
      overflow: hidden;
    }
    .blurred {
      filter: blur(5px);
      pointer-events: none;
    }
    .button-container {
      margin-bottom: 20px;
    }
    .toggle-button, .home-button {
      padding: 10px 20px;
      font-size: 1.2em;
      color: white;
      background-color: #ff7e5f;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s;
      margin: 5px;
    }
    .toggle-button:hover, .home-button:hover {
      background-color: #feb47b;
    }
    .cards-grid {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      width: 100%;
      max-width: 1200px;
      margin-bottom: 50px; /* Add margin at the bottom for future content */
    }
    .container {
      perspective: 1000px;
      display: inline-block;
      margin: 15px;
    }
    .card {
      width: 200px; /* Adjusted for better fitting */
      height: 280px; /* Adjusted for better fitting */
      position: relative;
      transform-style: preserve-3d;
      transition: transform 0.6s, width 0.6s, height 0.6s;
    }
    .card.flipped {
      transform: rotateY(180deg);
    }
    .card.zoomed {
      transform: scale(2);
      z-index: 1000;
    }
    .card .front, .card .back {
      position: absolute;
      width: 100%;
      height: 100%;
      backface-visibility: hidden;
      border-radius: 8px;
      top: 0;
      left: 0;
    }
    .card .front {
      background: linear-gradient(to right, #ff7e5f, #feb47b);
      z-index: 2;
      transform: rotateY(0deg);
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      color: white;
    }
    .card .back {
      background-color: white;
      transform: rotateY(180deg);
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      color: black;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      border: 1px solid #ccc;
    }
    .card h2 {
      font-size: 1.3em;
      margin: 13px 0;
      padding: 0;
    }
    .card p {
      font-size: 1em;
      margin: 6.5px 13px;
    }
    p.goal-desc {
      font-size: smaller;
    }
    .class-code {
      position: absolute;
      bottom: 20px;
      font-size: 1.2em;
      color: #a04d38;
      background-color: #fff;
      padding: 10px;
      border-radius: 5px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    .main-title {
      text-align: center;
      margin-bottom: 20px;
    }
    .main-title h1 {
      font-size: 2.5em;
      color: #ff7e5f;
      margin: 5px 0;
    }
    .main-title h2 {
      font-size: 1.8em;
      color: #ff7e5f;
      margin: 5px 0;
    }
    .main-title h3 {
      font-size: 1.5em;
      color: #ff7e5f;
      margin: 5px 0;
    }
  </style>
</head>
<body>
  <div class="main-title">
    <h1>Future Of Work</h1>
    <h2>Sustainability Innovation Camp</h2>
    <h3>Groups</h3>
  </div>
  <div class="button-container">
    <button class="toggle-button" onclick="toggleFlip()">Flip Card</button>
    <button class="home-button" onclick="window.location.href='/'">Home</button>
  </div>
  <div class="cards-grid">
    <% groups.forEach((group, index) => { %>
      <div class="container">
        <div class="card" id="card-<%= index %>">
          <div class="front"></div>
          <div class="back">
            <h2><%= group.name %></h2>
            <% if (group.problem) { %>
              <p><%= group.problem.problem_statement %></p>
              <p><strong>SDG:</strong> <%= group.problem.SDG %></p>
              <p class="goal-desc"><strong>Goal:</strong> <%= group.problem.goalTitle %></p>
            <% } else { %>
              <p>No problem assigned</p>
            <% } %>
          </div>
        </div>
      </div>
    <% }); %>
  </div>
  <div class="class-code">
    Class Code: <%= code %>
  </div>
  <script>
    function toggleFlip() {
      const cards = document.querySelectorAll('.card');
      cards.forEach(card => {
        card.classList.toggle('flipped');
      });
    }

    document.querySelectorAll('.card').forEach(card => {
      card.addEventListener('mouseover', handleMouseOver);
      card.addEventListener('mouseleave', handleMouseLeave);
    });

    function handleMouseOver(event) {
      const card = event.currentTarget;
      if (card.classList.contains('flipped')) {
        card.classList.add('zoomed');
        document.querySelectorAll('.card:not(.zoomed)').forEach(c => {
          c.classList.add('blurred');
        });
      }
    }

    function handleMouseLeave(event) {
      const card = event.currentTarget;
      card.classList.remove('zoomed');
      document.querySelectorAll('.card').forEach(c => {
        c.classList.remove('blurred');
      });
    }
  </script>
</body>
</html>
